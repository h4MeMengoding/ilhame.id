// Font loaded via CSS import in globals.css
// Using Google Fonts CDN to avoid build-time download issues in Docker
export const jakartaSans = {
  variable: '--jakartaSans-font',
  className: 'font-jakarta',
};
